Get all types of sensors for a user from the database
---
tags:
- Sensors
produces:
- application/json
responses:
  '200':
    description: Returns all sensor types currently registered on the system that are available for the user
    schema:
      properties:
        types:
          type: array
          items:
            type: string
            description: Type name
            example: 'Temperatura'
